services:
  workbench:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_DATA_DIR: /data
    volumes:
      - underwriting-data:/data
    restart: unless-stopped

volumes:
  underwriting-data:

